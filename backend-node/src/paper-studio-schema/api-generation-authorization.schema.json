{
  "$id": "https://localminidrama.local/schemas/paper-studio/api-generation-authorization-v1.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["request_id", "expected_version", "quote_fingerprint", "confirmed"],
  "properties": {
    "request_id": { "type": "string", "pattern": "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" },
    "expected_version": { "type": "integer", "minimum": 1 },
    "quote_fingerprint": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "confirmed": { "const": true },
    "shot_ids": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "uniqueItems": true,
      "items": { "type": "integer", "minimum": 1 }
    },
    "slot_ids": {
      "type": "array",
      "minItems": 1,
      "maxItems": 256,
      "uniqueItems": true,
      "items": { "type": "integer", "minimum": 1 }
    }
  }
}
